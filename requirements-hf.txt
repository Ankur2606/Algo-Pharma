# AlgoPharma — HF Spaces requirements
# torch and en-core-web-sm are installed separately in the Dockerfile
# to avoid the +cpu version tag issue with standard pip.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
python-multipart>=0.0.27
starlette>=1.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.49
psycopg2-binary>=2.9.10
alembic>=1.18.4

# ── Auth ──────────────────────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.5.0
bcrypt>=4.0.0

# ── Config ────────────────────────────────────────────────────────────────────
pydantic>=2.13.3
pydantic-settings>=2.14.0
python-dotenv>=1.2.2

# ── NLP / ML ──────────────────────────────────────────────────────────────────
# torch is installed separately (CPU wheel from download.pytorch.org)
transformers>=5.8.0
spacy>=3.8.14
scipy>=1.17.1
numpy>=2.4.4
vadersentiment>=3.3.2
langdetect>=1.0.9

# ── AI APIs ───────────────────────────────────────────────────────────────────
openai>=2.34.0
groq>=0.28.0
google-genai>=1.74.0
sarvamai>=0.1.28
httpx>=0.28.1

# ── Data / Crawling ───────────────────────────────────────────────────────────
apify-client>=2.5.0
firecrawl-py>=4.24.0
requests>=2.33.0

# ── Task queue (optional — celery worker runs separately) ─────────────────────
celery[redis]>=5.6.3
redis>=6.4.0

# ── Utilities ─────────────────────────────────────────────────────────────────
mcp>=1.0.0
openmed[hf]>=1.3.0
aiohttp>=3.13.5
python-dateutil>=2.9.0
rich>=15.0.0
